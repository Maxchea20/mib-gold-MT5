{
  "identifier": "default",
  "description": "mib-gold default capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-kill",
    { "identifier": "shell:allow-execute", "allow": [{ "name": "binaries/mibgold-backend", "sidecar": true }] }
  ]
}
